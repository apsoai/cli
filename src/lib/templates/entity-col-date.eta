<% const {field, isGql} = it; %>

<% if (isGql === true) {%>
    @Field(() => Date)
<% } %>
<% else {%>
    @Column({ type: 'date', nullable: <%= Boolean(field.nullable) %> })
    <% if (field.index ) {%>
    @Index()
    <% } %>
<% } %>

<%= field.name %>: string;