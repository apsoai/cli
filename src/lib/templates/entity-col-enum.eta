<% const {field} = it; %>

@Column({
    type: '<%= field.type %>',
    enum: [<%~ field.values?.map(s => `'${s}'`).join(',') %>],
    default: '<%~ field.default %>'
})
<% if (field.index ) {%>
@Index()
<% } %>
<%= field.name %>!: <%= field.dataType %>;