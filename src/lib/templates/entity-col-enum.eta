<% const {field} = it; %>

@Column({
    type: '<%= field.type %>',
    enum: [<%~ field.values?.map(s => `'${s}'`).join(',') %>],
    default: ['<%~ field.values[0] %>']
})
<%= field.name %>!: <%= field.dataType %>;