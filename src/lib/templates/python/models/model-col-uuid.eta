    <%= it.field.name %>: Mapped[<% if (it.field.nullable !== false) { %>Optional[UUID]<% } else { %>UUID<% } %>] = mapped_column(
        PGUUID(as_uuid=True),
        nullable=<%= it.field.nullable === false ? 'False' : 'True' %><% if (it.field.unique) { %>,
        unique=True<% } %>
    )
