    <%= it.field.name %>: Mapped[<% if (!it.field.required) { %>Optional[date]<% } else { %>date<% } %>] = mapped_column(
        Date,
        nullable=<%= it.field.required ? 'False' : 'True' %>
    )