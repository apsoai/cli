    <%= it.field.name %>: Mapped[<% if (!it.field.required) { %>Optional[str]<% } else { %>str<% } %>] = mapped_column(
        String(<%= it.field.length || 255 %>),
        nullable=<%= it.field.required ? 'False' : 'True' %><% if (it.field.unique) { %>,
        unique=True<% } %><% if (it.field.default !== undefined) { %>,
        default="<%= it.field.default %>"<% } %>
    )