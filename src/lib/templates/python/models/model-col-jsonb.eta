    <%= it.field.name %>: Mapped[<% if (it.field.nullable !== false) { %>Optional[dict]<% } else { %>dict<% } %>] = mapped_column(
        JSONB,
        nullable=<%= it.field.nullable === false ? 'False' : 'True' %>
    )