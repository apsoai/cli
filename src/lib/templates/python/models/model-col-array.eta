    <%= it.field.name %>: Mapped[<% if (it.field.nullable !== false) { %>Optional[list]<% } else { %>list<% } %>] = mapped_column(
        ARRAY(<%= it.field.arrayType === 'integer' ? 'Integer' : 'String' %>),
        nullable=<%= it.field.nullable === false ? 'False' : 'True' %>
    )