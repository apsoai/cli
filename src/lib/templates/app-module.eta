<%~ includeFile('./header.eta') %>

<% it.entities.forEach((entity) => { %>
import {<%= entity.name %>Module} from './<%= entity.name %>Module'
<%}) %>

export default [
<% it.entities.forEach((entity) => { %>
<%= entity.name %>Module,
<%}) %>
]
